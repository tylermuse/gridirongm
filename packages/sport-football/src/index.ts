/**
 * @bs/sport-football — BS Football sport adapter.
 *
 * Implements the SportAdapter contract from @bs/core/adapter for football.
 *
 * EMPTY at the moment. Will be populated during Phase 2 of the multi-sport
 * refactor, when the existing engine code in ../../src/lib/engine/ gets
 * migrated into this package and reshaped to satisfy the adapter contract.
 *
 * Reference for what this package will eventually contain:
 * see ../../adapter-spec/football.adapter.ts.
 */

export {};
